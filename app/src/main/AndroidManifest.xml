<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ricardoteixeira.movietvshowsexplorer">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".app.MoviesTvShowsApplication"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_cover_foreground"
        android:supportsRtl="true"
        android:theme="@style/Theme.MovieTVShowsExplorer">

        <activity android:name=".app.presentation.MainActivity"/>
        <activity android:name=".app.presentation.login.LoginActivity"
            android:noHistory="true"/>
        <activity android:name=".app.presentation.register.RegisterActivity"
            android:noHistory="true"/>
        <activity android:name=".app.presentation.welcome.WelcomeActivity"
            android:noHistory="true"/>

        <activity android:name=".app.presentation.splash.SplashActivity"
            android:noHistory="true">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
        </activity>

    </application>

</manifest>